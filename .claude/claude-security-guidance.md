# このプロジェクト固有のセキュリティルール

- ファイルアップロードは必ず S3 のプレサインド URL 経由で行う（直接アップロード禁止）
- DB クエリに使う userId は必ず next-auth のセッションから取得する（リクエストパラメータから取得しない）
- Server Action と API ルートでは必ず `auth()` でセッションを確認する（Server Action は例外を投げる、API ルートは 401 を返す）
